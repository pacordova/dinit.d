# Check all filesystems in fstab(5).
# This is interruptible with ^C

type = scripted
command = /bin/fsck -A -C -a
log-type = buffer
restart = false

options: starts-on-console start-interruptible skippable unmask-intr
before: mount@/
before: mount@-a
